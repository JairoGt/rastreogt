import 'package:flutter/material.dart';


class MotoPage extends StatelessWidget {
  const MotoPage({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Moto Page'),
      ),
    );
  }
}